info:
  name: actualizar
  type: http
  seq: 3

http:
  method: PATCH
  url: http://localhost:3000/tareas/:id/estado
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
